{
  "name": "tasky",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "env-cmd -f dev.env nodemon src/index.js",
    "test-local": "env-cmd -f test.env jest --coverage --forceExit",
    "test": "jest --coverage --forceExit --ci"
  },
  "dependencies": {
    "env-cmd": "^10.1.0",
    "express": "^4.17.1",
    "mongoose": "^5.9.10"
  },
  "devDependencies": {
    "jest": "^25.5.0",
    "nodemon": "^2.0.3",
    "supertest": "^4.0.2"
  },
  "jest": {
    "testEnvironment": "node",
    "modulePaths": [
      "<rootDir>/src"
    ]
  }
}
